### Example CI env file for integration tests
# Copy to .env.ci (local) or let CI create it.
# Placeholders — replace with real values in your CI secret store.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secret-placeholder
POSTGRES_DB=testdb

# Optional: tune as needed
#PGSSLMODE=disable
